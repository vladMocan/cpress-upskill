{
    "name": "cypress-up-skill",
    "version": "1.0.0",
    "description": "A demo project for end to end test using cypress",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "generate-report": "node scripts/cypress.js",
        "generate-report-firefox": "node scripts/cypress.js --browser firefox",
        "generate-report-electron": "node scripts/cypress.js --browser electron"
    },
    "author": "Vlad Mocan",
    "license": "ISC",
    "devDependencies": {
        "cypress": "^8.5.0",
        "mocha": "^9.1.2",
        "mochawesome": "^6.3.1",
        "mochawesome-merge": "^4.2.0",
        "mochawesome-report-generator": "^5.2.0"
    },
    "dependencies": {
        "fs-extra": "^10.0.0"
    }
}
